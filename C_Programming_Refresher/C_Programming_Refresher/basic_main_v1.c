// main.c

#include <stdio.h>
#include "utils.h" // Include the header file for utility functions

int main() {
    int result = add(6, 3); // Call the 'add' function declared in utils.h to calculate 2 + 3  or # + #
    printf("Result: %d\n", result); // Print the result
    return 0;
}
