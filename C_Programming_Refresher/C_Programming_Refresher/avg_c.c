#include <stdio.h>

void function() {
    //auto int i = 10;  // 'auto' keyword specifies that the variable 'i' has automatic storage. In practice, 'auto' is rarely used explicitly because it's the default for local variables.
    //printf("%d\n", i);
}

int main() {
    function();
    return 0;
}
